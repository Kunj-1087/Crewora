'use client';

// Re-exported here so data/UI hooks all live under src/hooks/.
// The implementation lives with the ToastProvider component.
export { useToast } from '@/components/ui/Toast';
export type { ToastType } from '@/components/ui/Toast';
